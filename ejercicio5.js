// Definimos una función llamada 'saludar' que toma un parámetro 'nombre'.
function saludar(nombre) {
    // Devuelve un saludo personalizado que incluye el nombre proporcionado.
    return "Hola, " + nombre;
}

// Llama a la función 'saludar' con el argumento "Diego" y muestra el resultado en la consola.
console.log(saludar("Diego"));
